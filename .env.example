# Copier ce fichier en .env et renseigner des valeurs reelles.
# .env ne doit jamais etre commit (voir .gitignore).

# DB_HOST doit correspondre au nom du service Postgres dans docker-compose.yml.
DB_HOST=postgres
DB_PORT=5432
DB_NAME=bibliotheque
DB_USERNAME=bibliotheque
DB_PASSWORD=change-me
